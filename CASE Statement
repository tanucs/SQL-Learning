----------------CASE STATEMENT------------

/*
Create report showing total sales for each of the following 
categories: High (sales over 50), Medium (sales 21-50),
Low(sales 20 or less) Sort the categories from highest
sales to lowest */

   ---Agggregate Data----
SELECT
Category,
SUM(Sales) AS TotalSales ----- Aggregate Data---------
FROM(
SELECT
OrderID,
Sales,
CASE
 WHEN Sales > 50 THEN 'High'
 WHEN Sales > 20 THEN 'Medium'
 ELSE 'Low'
END Category
FROM Sales.Orders
)t
GROUP BY Category
ORDER BY TotalSales DESC

SELECT 
OrderID,
Sales,
CASE 
   WHEN Sales > 50 THEN 'High'
   WHEN Sales > 20 THEN 'Medium'
END Category
FROM Sales.Orders
